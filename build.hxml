# Most recent release (at time of writing)
-lib ufront:1.0.0-rc.6
# 'server' directive necessary for compilation of server-side logic
-D server
# Server class as main entry point, targeting PHP
-cp src
-main Server
-php www
# For generating documentation with dox
-xml project.xml